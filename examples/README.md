# Examples

| Script | Purpose |
|--------|---------|
| `vllm_dev_launcher.py` | Register `CUSTOM` (`TURBOQUANT_DEV`) then run `vllm` CLI in-process |
| `sglang_dev_launcher.py` | Register `turboquant_dev` then run SGLang `launch_server` logic |

Step-by-step narrative: [docs/tutorial-inference-integrations.md](../docs/tutorial-inference-integrations.md).
